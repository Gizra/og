version: "2"

prepare:
  fetch:
  - url: "https://raw.githubusercontent.com/digipolisgent/ci_config-files-and-scripts/master/php/drupal8/.phpmd.xml"
    path: ".phpmd.xml"
  - url: "https://git.drupalcode.org/project/drupal/raw/8.7.x/.csslintrc"
    path: ".csslintrc"
  - url: "https://git.drupalcode.org/project/drupal/raw/8.7.x/core/.eslintrc.json"
    path: ".eslintrc.json"
  - url: "https://git.drupalcode.org/project/drupal/raw/8.7.x/core/.eslintignore"
    path: ".eslintignore"

checks:
  argument-count:
    enabled: false
  complex-logic:
    enabled: false
  file-lines:
    enabled: false
  method-complexity:
    enabled: false
  method-count:
    enabled: false
  method-lines:
    enabled: false
  nested-control-flow:
    config:
      threshold: 16
  return-statements:
    config:
      threshold: 16
  similar-code:
    config:
      threshold: # language-specific defaults. an override will affect all languages.
  identical-code:
    config:
      threshold: # language-specific defaults. an override will affect all languages.

plugins:
  phpcodesniffer:
    enabled: true
    config:
      standard: "Drupal,DrupalPractice"
      file_extensions: "php,inc,module,install"
  phpmd:
    enabled: true
    config:
      file_extensions:
        - php
        - inc
        - module
        - install
      rulesets: ".phpmd.xml"
  csslint:
    enabled: true
  eslint:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        php:
        javascript:
  fixme:
    enabled: true
  markdownlint:
    enabled: true
    issue_override:
      severity: minor

exclude_patterns:
- "config/"
- "translations/"
- "**/vendor/**/*"
- "**/.idea/**/*"
- "**/tests/**/*"
- "**/*.settings.php"
