language: php

php:
  - 5.3

mysql:
  database: drupal
  username: root
  encoding: utf8

before_script:
  # navigate out of module directory to prevent blown stack by recursive module lookup
  - docker build -t og .
  - $TRAVIS_PULL_REQUEST_BRANCH

script: docker run --name og-con -p 8080:80 -d og
