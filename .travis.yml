sudo: required
language: php

services:
  - docker

before_script:
  # navigate out of module directory to prevent blown stack by recursive module lookup
  - docker build -t og .

script: docker run --name og-con -e BRANCH=$TRAVIS_PULL_REQUEST -p 8080:80 -d og
