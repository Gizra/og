<?php
// $Id: $

/**
 * @file
 * Example module to show integration between OG and panels, and serve as a
 * starting point to customize group display.
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function og_panels_example_ctools_plugin_api($module, $api) {
  if ($module == 'page_manager' && $api == 'pages_default') {
    return array('version' => 1);
  }
}
